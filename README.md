# Citrus Chat Mobile
----

- 📱 Available for Android 10+

## For developing

- Clone the repository.
- Use `git config core.hooksPath hooks/` to set up the hooks.
- Don't develop on the `main` branch, create a new branch for your work.

package:mine -tag:Insets -tag:InputManagerGlobal -tag:ViewRootImpl -tag:IMETRACKER -tag:Input -tag:OpenGL -tag:BLAST -tag:Window -tag:Choreographer -tag:Native -tag:CompatibilityChangeReporter 